# ══════════════════════════════════════════════════════════════════
#  requirements.txt — Tunisia Price Hunter
#  Dépendances communes à tous les services
#
#  Chaque service installe CE fichier complet.
#  Les bibliothèques inutilisées par un service restent légères
#  car elles sont déjà présentes dans l'image de base Python.
# ══════════════════════════════════════════════════════════════════

# ── Web framework (tous les services) ────────────────────────────
fastapi==0.122.0
uvicorn==0.38.0
starlette==0.50.0
pydantic==2.11.5
pydantic-settings==2.13.1
playwright
# ── Base de données (app + services 1/2/3) ───────────────────────
sqlalchemy==2.0.46
psycopg2-binary==2.9.9

# ── Auth / Sécurité (app principale) ─────────────────────────────
python-jose==3.5.0
passlib[bcrypt]==1.7.4
email-validator==2.3.0
slowapi==0.1.9

# ── Scraping HTTP (service 1 + temps réel) ───────────────────────
httpx==0.27.0
selectolax==0.3.17
curl_cffi==0.7.3

# ── Machine Learning (service 2) ─────────────────────────────────
numpy==2.4.2
scikit-learn==1.8.0

# ── Scheduler + Email (service 3) ────────────────────────────────
apscheduler==3.11.2
aiosmtplib==3.0.2

# ── Utilitaires (tous les services) ──────────────────────────────
python-dotenv==1.2.1